<Window x:Class="XLinkViewer.Views.ChecklistWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:XLinkViewer.Views"
        mc:Ignorable="d"
        Title="Create Drawing Revision Checklist" 
        Height="500" Width="600"
        WindowStartupLocation="CenterOwner"
        ResizeMode="CanResize">
    
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <!-- Header Section -->
        <StackPanel Grid.Row="0" Margin="0,0,0,15">
            <TextBlock Text="Create Revision Checklist" FontSize="18" FontWeight="Bold" Margin="0,0,0,10"/>
            <TextBlock Name="SourceDrawingText" FontSize="14" Foreground="DarkBlue"/>
        </StackPanel>
        
        <!-- Instructions -->
        <TextBlock Grid.Row="1" Text="Select the drawings you want to include in your revision checklist:" 
                   FontStyle="Italic" Margin="0,0,0,10"/>
        
        <!-- Drawing Selection List -->
        <GroupBox Grid.Row="2" Header="Dependent Drawings" Margin="0,0,0,15">
            <Grid>
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <StackPanel Name="DrawingSelectionPanel">
                        <!-- Checkboxes will be added dynamically -->
                    </StackPanel>
                </ScrollViewer>
                
                <!-- No items message -->
                <TextBlock Name="NoItemsMessage" 
                           Text="No dependent drawings found. This drawing is not referenced by any other drawings." 
                           HorizontalAlignment="Center" VerticalAlignment="Center"
                           Foreground="Gray" FontStyle="Italic" FontSize="14"
                           Visibility="Collapsed"/>
            </Grid>
        </GroupBox>
        
        <!-- Output Options -->
        <GroupBox Grid.Row="3" Header="Output Options" Margin="0,0,0,15">
            <StackPanel>
                <RadioButton Name="TxtFormat" Content="Text File (.txt)" IsChecked="True" Margin="0,5"/>
                <RadioButton Name="MdFormat" Content="Markdown File (.md)" Margin="0,5"/>
                <CheckBox Name="IncludeTimestamp" Content="Include timestamp in filename" IsChecked="True" Margin="0,5"/>
            </StackPanel>
        </GroupBox>
        
        <!-- Action Buttons -->
        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right">
            <TextBlock Name="SelectionCount" VerticalAlignment="Center" Margin="0,0,15,0" Foreground="Gray"/>
            <Button Content="Select All" Name="SelectAllButton" Padding="10,5" Margin="0,0,10,0" Click="SelectAllButton_Click"/>
            <Button Content="Clear All" Name="ClearAllButton" Padding="10,5" Margin="0,0,10,0" Click="ClearAllButton_Click"/>
            <Button Content="Generate Checklist" Name="GenerateButton" Padding="15,5" Margin="0,0,10,0" 
                    Click="GenerateButton_Click" IsDefault="True"/>
            <Button Content="Cancel" Name="CancelButton" Padding="15,5" Click="CancelButton_Click" IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>